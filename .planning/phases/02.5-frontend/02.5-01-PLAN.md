---
phase: "02.5"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/postcss.config.js
  - frontend/tailwind.config.ts
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/lib/utils.ts
  - frontend/src/vite-env.d.ts
autonomous: true
requirements:
  - UI-01
user_setup: []
must_haves:
  truths:
    - "Project builds with `npm run build` producing no errors"
    - "Tailwind v3 is configured with postcss pipeline"
    - "shadcn/ui is initialized with button component added"
    - "Vite dev server proxies /api requests to localhost:8000"
    - "Dark class is set on <html> before React hydrates (no flash)"
    - "cn() utility function exists in src/lib/utils.ts"
  artifacts:
    - path: "frontend/package.json"
      provides: "Exact dependency versions pinned"
      contains: "\"react\": \"^18\""
    - path: "frontend/vite.config.ts"
      provides: "Vite config with proxy and @ alias"
      contains: "proxy"
    - path: "frontend/tailwind.config.ts"
      provides: "Tailwind v3 dark mode class strategy + content paths"
      contains: "darkMode: \"class\""
    - path: "frontend/src/index.css"
      provides: "Tailwind directives + CSS variables"
      contains: "@tailwind base"
    - path: "frontend/index.html"
      provides: "Root HTML with dark class script"
      contains: "document.documentElement.classList.add('dark')"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() utility for class merging"
      contains: "export function cn"
  key_links:
    - from: "vite.config.ts"
      to: "backend (localhost:8000)"
      via: "proxy target"
      pattern: "target:.*8000"
    - from: "index.html"
      to: "src/main.tsx"
      via: "<script type=\"module\" src=\"/src/main.tsx\">"
      pattern: "src=\"/src/main.tsx\""
---

<objective>
Scaffold the PaperTrade frontend project with Vite 5 + React 18 + TypeScript 5 + TailwindCSS v3 + shadcn/ui.

Purpose: Create the foundation that all subsequent plans build on. Every file created here is a prerequisite for every other plan.
Output: A working Vite dev server that serves a dark-themed React app with Tailwind, shadcn components, and API proxy to the backend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.5-frontend/02.5-CONTEXT.md
@.planning/phases/02.5-frontend/02.5-RESEARCH.md
@.planning/phases/02.5-frontend/02.5-UI-SPEC.md

Architecture patterns from RESEARCH.md:
- Vite config with proxy to localhost:8000 (Pattern 5, lines 643-656)
- Zustand v5 `create<State>()()` double-parenthesis convention (lines 665-675)
- shadcn v3-compatible init (lines 682-684)
- Dark mode flicker prevention script in index.html (lines 693-699)
- Recommended project structure (lines 227-297)
- Tailwind dark mode class strategy config (lines 858-890)

**Key versions (pinned per RESEARCH.md):**
- react@18.3.1, react-dom@18.3.1
- react-router-dom@6.30.3
- @tanstack/react-query@5.100.6
- zustand@5.0.12
- vite@5.4.21, tailwindcss@3.4.19
- typescript@5.9.3, @vitejs/plugin-react@6.0.1
- lucide-react@1.14.0
- shadcn CLI latest (detects Tailwind v3)

**shadcn init settings (per UI-SPEC):**
- Style: default (not New York)
- Base color: zinc
- CSS variables: yes
- Dark mode: yes (class strategy)
- TailwindCSS prefix: none

**shadcn components to add:**
button, card, badge, input, select, dialog, table, tooltip, sheet, toast, dropdown-menu, separator, skeleton, tabs

**Backend endpoint (Vite proxy target):** http://localhost:8000
**Proxy paths:** /api (REST), /ws (WebSocket)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project and install all dependencies</name>
  <files>
    frontend/package.json
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/tsconfig.node.json
    frontend/vite.config.ts
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/vite-env.d.ts
  </files>
  <read_first>
    frontend/ (directory must not exist — create fresh)
    .planning/phases/02.5-frontend/02.5-RESEARCH.md (lines 125-160 for exact versions)
    .planning/phases/02.5-frontend/02.5-CONTEXT.md (for locked decisions)
  </read_first>
  <action>
    Create the frontend directory at /home/pichuu/Desktop/PaperTrade/frontend.

    Step 1: Create Vite 5 React TypeScript project:
    ```
    npm create vite@5 frontend -- --template react-ts
    cd frontend
    ```

    Step 2: Pin exact versions in package.json. Core dependencies:
    - react@^18.3.1, react-dom@^18.3.1
    - react-router-dom@^6.30.3
    - @tanstack/react-query@^5.100.6
    - zustand@^5.0.12
    - lucide-react@^1.14.0

    Dev dependencies:
    - vite@^5.4.21
    - @vitejs/plugin-react@^6.0.1
    - typescript@^5.9.3
    - @types/react@^18.3.0
    - @types/react-dom@^18.3.0
    - tailwindcss@^3.4.19
    - postcss@^8.5.0
    - autoprefixer@^10.4.20
    - clsx@^2.1.1
    - tailwind-merge@^2.6.0

    Run: npm install

    Step 3: Strip the default Vite template content. Delete src/App.css and remove any Vite demo content from src/App.tsx (replace with empty shell).

    Step 4: Update tsconfig.json — enable strict mode:
    ```json
    {
      "compilerOptions": {
        "target": "ES2020",
        "useDefineForClassFields": true,
        "lib": ["ES2020", "DOM", "DOM.Iterable"],
        "module": "ESNext",
        "skipLibCheck": true,
        "moduleResolution": "bundler",
        "allowImportingTsExtensions": true,
        "isolatedModules": true,
        "moduleDetection": "force",
        "noEmit": true,
        "jsx": "react-jsx",
        "strict": true,
        "noUnusedLocals": true,
        "noUnusedParameters": true,
        "noFallthroughCasesInSwitch": true,
        "noUncheckedSideEffectImports": true,
        "paths": {
          "@/*": ["./src/*"]
        }
      },
      "include": ["src"]
    }
    ```

    Step 5: Create vite.config.ts with proxy and @ alias (per RESEARCH.md Pattern 5, lines 643-656):
    ```typescript
    import { defineConfig } from "vite";
    import react from "@vitejs/plugin-react";
    import path from "path";

    export default defineConfig({
      plugins: [react()],
      resolve: {
        alias: {
          "@": path.resolve(__dirname, "./src"),
        },
      },
      server: {
        proxy: {
          "/api": {
            target: "http://localhost:8000",
            changeOrigin: true,
          },
          "/ws": {
            target: "ws://localhost:8000",
            ws: true,
          },
        },
      },
    });
    ```

    Step 6: Update index.html — add dark mode flicker prevention script BEFORE the module script (per RESEARCH.md lines 693-699):
    ```html
    <!doctype html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <link rel="icon" type="image/svg+xml" href="/vite.svg" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>PaperTrade</title>
        <script>
          document.documentElement.classList.add('dark');
        </script>
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    Step 7: Create src/main.tsx as minimal shell (will be expanded in Plan 02):
    ```typescript
    import { createRoot } from "react-dom/client";
    import App from "./App";
    import "./index.css";

    createRoot(document.getElementById("root")!).render(<App />);
    ```

    Step 8: Create src/App.tsx as minimal shell:
    ```typescript
    export default function App() {
      return <div className="min-h-screen bg-background text-primary"><h1>PaperTrade</h1></div>;
    }
    ```

    Step 9: Create src/vite-env.d.ts:
    ```typescript
    /// <reference types="vite/client" />
    ```

    Step 10: Remove default Vite template files:
    - Delete src/App.css if it exists
    - Delete src/assets/ if it exists
  </action>
  <verify>
    <automated>cd /home/pichuu/Desktop/PaperTrade/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` exits 0 with no TypeScript errors.
    package.json contains all pinned dependencies.
    vite.config.ts has proxy to localhost:8000.
    index.html has dark mode flicker prevention script.
  </done>
  <acceptance_criteria>
    - package.json contains "vite" with value "^5" and "react" with value "^18"
    - vite.config.ts contains 'target: "http://localhost:8000"' and 'ws: true'
    - index.html contains 'document.documentElement.classList.add("dark")'
    - tsconfig.json contains '"strict": true' and '"paths": { "@/*": ["./src/*"] }'
    - npm run build exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Configure TailwindCSS v3, PostCSS, and shadcn/ui</name>
  <files>
    frontend/postcss.config.js
    frontend/tailwind.config.ts
    frontend/src/index.css
    frontend/src/lib/utils.ts
  </files>
  <read_first>
    frontend/package.json (must have tailwindcss@3, postcss, autoprefixer installed)
    .planning/phases/02.5-frontend/02.5-RESEARCH.md (lines 855-890 for Tailwind config pattern, lines 790-801 for cn utility and isPriceStale)
    .planning/phases/02.5-frontend/02.5-UI-SPEC.md (lines 108-186 for color palette, lines 52-67 for spacing scale)
  </read_first>
  <action>
    Step 1: Create postcss.config.js:
    ```javascript
    export default {
      plugins: {
        tailwindcss: {},
        autoprefixer: {},
      },
    };
    ```

    Step 2: Create tailwind.config.ts with dark mode class strategy and the UI-SPEC color palette:
    ```typescript
    import type { Config } from "tailwindcss";

    const config: Config = {
      darkMode: "class",
      content: ["./index.html", "./src/**/*.{ts,tsx}"],
      theme: {
        extend: {
          colors: {
            background: "#0B0D10",
            foreground: "#EDEEF0",
            surface: {
              1: "#13161C",
              2: "#1A1E27",
              3: "#232833",
            },
            primary: {
              DEFAULT: "#EDEEF0",
              foreground: "#0B0D10",
            },
            secondary: {
              DEFAULT: "#A0A5B0",
              foreground: "#0B0D10",
            },
            muted: {
              DEFAULT: "#6B7280",
              foreground: "#EDEEF0",
            },
            info: {
              DEFAULT: "#3B82F6",
              foreground: "#FFFFFF",
            },
            positive: {
              DEFAULT: "#22C55E",
              foreground: "#000000",
              dim: "#166534",
            },
            negative: {
              DEFAULT: "#EF4444",
              foreground: "#FFFFFF",
              dim: "#7F1D1D",
            },
            warning: {
              DEFAULT: "#F59E0B",
              foreground: "#000000",
              dim: "#78350F",
            },
            destructive: {
              DEFAULT: "#DC2626",
              foreground: "#FFFFFF",
            },
            border: {
              DEFAULT: "#2A2F3B",
              subtle: "#1F232E",
            },
          },
          fontFamily: {
            sans: ["Inter", "system-ui", "sans-serif"],
          },
        },
      },
      plugins: [],
    };

    export default config;
    ```

    Step 3: Create src/index.css with Tailwind directives and CSS variable architecture (per UI-SPEC lines 110-186 for color values):
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    @layer base {
      :root {
        --background: 220 20% 5%;
        --foreground: 220 8% 93%;
        --card: 220 18% 9%;
        --card-foreground: 220 8% 93%;
        --popover: 220 18% 13%;
        --popover-foreground: 220 8% 93%;
        --primary: 220 8% 93%;
        --primary-foreground: 220 20% 5%;
        --secondary: 220 8% 66%;
        --secondary-foreground: 220 20% 5%;
        --muted: 220 8% 46%;
        --muted-foreground: 220 8% 93%;
        --accent: 217 91% 60%;
        --accent-foreground: 0 0% 100%;
        --destructive: 0 72% 51%;
        --destructive-foreground: 0 0% 100%;
        --info: 217 91% 60%;
        --info-foreground: 0 0% 100%;
        --positive: 142 71% 45%;
        --positive-foreground: 0 0% 0%;
        --negative: 0 84% 60%;
        --negative-foreground: 0 0% 100%;
        --warning: 38 92% 50%;
        --warning-foreground: 0 0% 0%;
        --border: 220 14% 20%;
        --input: 220 14% 20%;
        --ring: 217 91% 60%;
        --radius: 0.5rem;
      }

      * {
        @apply border-border;
      }

      body {
        @apply bg-background text-primary;
        font-feature-settings: "rlig" 1, "calt" 1;
      }
    }

    @layer utilities {
      .text-balance {
        text-wrap: balance;
      }
    }

    /* prefers-reduced-motion: disable all animations */
    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
      }
    }
    ```

    Step 4: Install Inter font. Add this to index.html <head>:
    ```html
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />
    ```

    Step 5: Create src/lib/utils.ts with cn() utility (per RESEARCH.md lines 790-793):
    ```typescript
    import { clsx, type ClassValue } from "clsx";
    import { twMerge } from "tailwind-merge";

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs));
    }
    ```

    Step 6: Initialize shadcn/ui:
    ```bash
    cd /home/pichuu/Desktop/PaperTrade/frontend
    npx shadcn@latest init
    ```
    Answer prompts:
    - Style: default
    - Base color: Zinc
    - CSS variables: Yes
    - Dark mode: Yes (class)
    - TailwindCSS prefix: (none/empty)

    Step 7: Add shadcn components:
    ```bash
    npx shadcn@latest add button card badge input select dialog table tooltip sheet toast dropdown-menu separator skeleton tabs
    ```

    Step 8: Verify the build still passes after adding shadcn components.

    Step 9: Generate the @/components/ui components.json path correctly. After shadcn init, the components.json should show "tailwind": {"css": "src/index.css", "config": "tailwind.config.ts", "prefix": ""} and "aliases": {"utils": "@/lib/utils", "components": "@/components/ui"}.
  </action>
  <verify>
    <automated>cd /home/pichuu/Desktop/PaperTrade/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    npm run build exits 0 with shadcn components.
    tailwind.config.ts has darkMode: "class" and all UI-SPEC color tokens.
    src/index.css has @tailwind directives and CSS custom properties.
    src/components/ui/button.tsx exists (from shadcn).
    src/lib/utils.ts exports cn() function.
  </done>
  <acceptance_criteria>
    - tailwind.config.ts contains 'darkMode: "class"'
    - tailwind.config.ts contains '"#0B0D10"' (background color)
    - tailwind.config.ts contains '"#22C55E"' (positive color)
    - tailwind.config.ts contains '"Inter"' font family
    - src/index.css contains '@tailwind base' directive
    - src/lib/utils.ts contains 'export function cn'
    - src/components/ui/button.tsx exists
    - src/components/ui/card.tsx exists
    - src/components/ui/badge.tsx exists
    - components.json exists with correct aliases
    - npm run build exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline -> Browser | Untrusted npm packages execute during build; Vite sandboxes dev server |
| Browser -> Backend API | All API calls proxied through Vite in dev; CORS already configured on backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.5-01 | T (Tampering) | npm dependencies | accept | Local dev project; no supply chain protection needed for v1 prototype |
| T-2.5-02 | I (Info Disclosure) | Vite dev server | accept | Dev-only; no sensitive data in frontend bundle |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run dev` starts without errors
- Browser loads http://localhost:5173 and shows dark background with "PaperTrade" text (no 404, no blank white flash)
</verification>

<success_criteria>
- Frontend project compiles and serves at localhost:5173
- Tailwind dark mode renders correctly
- shadcn/ui button component exists and can be rendered
- Vite proxy forwards /api/* to localhost:8000
- cn() utility function is exported and usable
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-frontend/02.5-01-SUMMARY.md`
</output>
