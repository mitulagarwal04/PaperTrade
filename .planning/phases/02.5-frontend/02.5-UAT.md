---
status: testing
phase: 02.5-frontend
source:
  - 02.5-01-SUMMARY.md
  - 02.5-02-SUMMARY.md
  - 02.5-03-SUMMARY.md
  - 02.5-04-SUMMARY.md
  - 02.5-05-SUMMARY.md
  - 02.5-06-SUMMARY.md
started: 2026-05-01T01:06:00+05:30
updated: 2026-05-01T01:06:00+05:30
---

## Current Test

[testing complete — see manual checklist below]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running frontend. `npm run dev` in frontend/ starts on localhost:5173. App loads with dark background, layout shell.
result: pass
checks:
  - "Build: npm run build exits 0 (2.21s, no errors)"
  - "Dev server: Vite ready in 131ms, HTTP 200 on localhost:5173"
  - "All routes: / /dashboard /portfolio /orders /assets /settings /nonexistent all return 200"
  - "Index.html: dark mode flicker script present, Inter font, module entry"
  - "Vite proxy: /api/v1/portfolio and /api/v1/assets return real backend data"

### 2. Desktop Sidebar Navigation
expected: On screens >=1024px, left sidebar shows 5 nav items (Dashboard, Portfolio, Orders, Assets, Settings) with Lucide icons. Active route highlighted with bg-surface-3 and left border. Sidebar collapses to 56px via toggle button.
result: manual

### 3. Mobile Bottom Tab Bar
expected: On screens <1024px, fixed bottom bar shows 5 tabs with icon + text-[11px] label. Active tab in text-info, inactive in text-muted.
result: manual

### 4. Header Route Title
expected: Header shows correct title for each page — "Dashboard" at /, "Portfolio" at /portfolio, "Orders" at /orders, "Assets" at /assets, "Settings" at /settings.
result: manual

### 5. 404 Page
expected: Navigating to /nonexistent-route shows full-page centered "Page not found" with links to Dashboard, Portfolio, and Orders.
result: manual

### 6. Dashboard Landing Page
expected: Route / loads Dashboard page showing portfolio summary cards (Cash Balance, Total Value, Unrealized P&L, Win Rate) with ₹ formatting and en-IN locale.
result: manual

### 7. Portfolio Summary Cards
expected: Portfolio page (/portfolio) shows 4 metric cards from usePortfolio hook. P&L values colored green (positive) or red (negative) with ₹ symbol.
result: manual

### 8. Positions Table (Empty State)
expected: When no positions exist, table shows "No positions yet" with "Place your first trade" button linking to /orders.
result: manual

### 9. Positions Table (With Data)
expected: When positions exist, table renders sortable columns (Symbol, Qty, Avg Cost, Current Price, Market Value, P&L) with ▲▼ sort indicators. P&L columns include TrendingUp/TrendingDown icons.
result: manual
note: "Current backend returns empty positions array. Creating a position first is required for this test."

### 10. Orders Table
expected: Orders page (/orders) shows order history table with sortable columns and colored status badges. Status colors: green for FILLED, red for REJECTED, blue for PENDING/OPEN, yellow for PARTIAL_FILLED, muted for CANCELLED/EXPIRED.
result: manual

### 11. Order Form Side Panel
expected: Clicking "Place Order" opens a shadcn Sheet side panel with BUY/SELL toggle, order type Select (MARKET/LIMIT/STOP), conditional fields, and submit button. Success shows toast.
result: manual

### 12. Cancel Order Dialog
expected: Cancel button on PENDING/OPEN orders opens a shadcn Dialog with "Cancel Order" title, "Yes, Cancel Order" and "Keep Order" buttons.
result: manual
note: "Requires a PENDING/OPEN order. All current orders are FILLED or CANCELLED."

### 13. Assets Page
expected: Assets page (/assets) shows list of tracked assets with live prices from WebSocket, stale indicators (dimmed text + tooltip for stale, yellow badge for extended stale).
result: manual

### 14. Settings Page
expected: Settings page (/settings) shows portfolio reset button with confirmation dialog. Reset calls POST /api/v1/portfolio/reset and invalidates queries.
result: manual

### 15. WebSocket Reconnect Banner
expected: When WebSocket disconnects, a banner slides down from top between Header and content showing reconnection status with attempt count. On reconnect, banner slides up.
result: manual

## Summary

total: 15
passed: 1
issues: 0
pending: 0
manual: 14
skipped: 0
blocked: 0

## Gaps

[none yet]

## Manual Verification Checklist

Open http://localhost:5173 in a browser and check each item:

| # | Check | Pass? |
|---|-------|-------|
| M1 | **Dark theme renders** — background is dark, text is light, no flash on load | ☐ |
| M2 | **Sidebar: 5 nav items** — Dashboard, Portfolio, Orders, Assets, Settings with icons. Active route has left border highlight | ☐ |
| M3 | **Sidebar collapse** — Click collapse toggle (bottom of sidebar). Sidebar shrinks to icon-only (~56px). Toggle again to expand | ☐ |
| M4 | **Header title updates** — Navigate each route. Header shows "Dashboard", "Portfolio", "Orders", "Assets", "Settings" matching current page | ☐ |
| M5 | **Bottom tab bar (mobile)** — Resize browser <1024px. Bottom bar appears with 5 icon+label tabs. Active tab highlighted | ☐ |
| M6 | **Dashboard cards** — Route / shows 4 summary cards: Cash Balance (₹1,00,000.00), Total Value, Unrealized P&L, Win Rate. ₹ formatting with en-IN locale | ☐ |
| M7 | **Portfolio page** — /portfolio shows same cards with "Positions" section heading | ☐ |
| M8 | **Orders table** — /orders shows order history. Columns: ID, Symbol, Side, Type, Qty, Filled, Price, Status, Date, Actions. Status badges colored per spec (green FILLED, muted CANCELLED) | ☐ |
| M9 | **Order column sorting** — Click column headers in orders table. Rows re-sort, arrow indicator shows direction | ☐ |
| M10 | **Place Order panel** — Click "Place Order". Sheet slides in from right with BUY/SELL toggle, order type Select (MARKET/LIMIT/STOP), symbol/quantity/price fields | ☐ |
| M11 | **Assets page** — /assets shows AAPL, MSFT, GOOGL, TSLA, BTC, ETH with current prices | ☐ |
| M12 | **Settings page** — /settings shows "Reset Portfolio" button. Click opens confirmation dialog | ☐ |
| M13 | **404 page** — Navigate to /nonexistent. Shows "Page not found" with links to Dashboard, Portfolio, Orders | ☐ |
| M14 | **Positions empty state** — /portfolio positions section shows "No positions yet" with "Place your first trade" button linking to /orders | ☐ |
| M15 | **WebSocket reconnect banner** — Stop backend (Ctrl+C on backend server). Banner appears "Connection lost, reconnecting (attempt N)". Restart backend. Banner disappears | ☐ |
| X1 | **No console errors** — Open DevTools console. No uncaught errors or React warnings | ☐ |
| X2 | **Responsive** — Resize from 375px to 1440px. Layout adapts: sidebar hides on mobile, bottom bar appears, cards reflow | ☐ |
| X3 | **Accessibility** — Tab through nav items. Focus rings visible. Buttons have hover states with smooth transitions | ☐ |
