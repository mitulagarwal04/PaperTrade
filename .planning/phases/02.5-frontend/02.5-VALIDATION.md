---
phase: 2.5
slug: frontend
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-30
---

# Phase 2.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | (none — frontend testing deferred to Phase 3) |
| **Config file** | (none — Phase 2.5) |
| **Quick run command** | `npm run dev` (smoke test) |
| **Full suite command** | Manual verification per UI-SPEC contract |
| **Estimated runtime** | ~N/A |

---

## Sampling Rate

- **After every task commit:** `npm run build` must succeed (TypeScript + Vite build)
- **After every plan wave:** Manual visual check of affected pages
- **Before `/gsd-verify-work`:** Full UI-SPEC dimension sign-off
- **Max feedback latency:** N/A (manual verification)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | 01 | 1 | UI-01 | — | No XSS (React escaping), form validation UX | manual | `npm run build` | ❌ W0 | ⬜ pending |
| TBD | 02 | 1 | UI-02 | — | Read-only portfolio display | manual | `npm run build` | ❌ W0 | ⬜ pending |
| TBD | 03 | 1 | UI-03 | — | Order form UX validation, WS display | manual | `npm run build` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `package.json` — project scaffold (Vite + React + TS + TailwindCSS)
- [ ] `tsconfig.json` — strict mode
- [ ] `vite.config.ts` — proxy to backend, tailwind plugin

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Responsive layout + nav | UI-01 | Visual/spatial verification | Open at 375px, 768px, 1024px, 1440px — nav adapts correctly |
| Dark theme rendering | UI-01 | Visual verification | All pages render in dark scheme with proper contrast |
| Portfolio summary + positions | UI-02 | Data display verification | Compare UI against backend API response for cash, P&L, positions |
| Order CRUD flow | UI-03 | End-to-end flow | Place market order → verify in open orders → cancel → verify removed |
| WS price stream | UI-03 | Real-time verification | Open devtools WS tab, confirm 5s price updates, UI reflects changes |
| Stale price indicator | UI-03 | Visual verification | Disconnect network, verify dimmed + tooltip appears |
| Empty states | UI-01 | Visual/spatial | Fresh portfolio shows "No positions yet" + CTA |

---

## Validation Sign-Off

- [ ] All UI-SPEC 6 dimensions pass visual audit
- [ ] `npm run build` succeeds with zero errors
- [ ] All 4 route pages render without console errors
- [ ] WebSocket connects on page load and displays prices
- [ ] Order placement flow completes end-to-end
- [ ] Responsive at 375px, 768px, 1024px, 1440px
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
